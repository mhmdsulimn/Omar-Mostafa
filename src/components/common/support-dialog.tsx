'use client';

// This component is no longer used and has been replaced by the /dashboard/support page.
// The file is kept to avoid breaking changes but its content is cleared.
export function SupportDialog({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
